var = 'hello to external_lib library'
print(var)

import .std.math
random_number_result = math.random_number()
assert(random_number_result == 4)

import std.math
import std.types

assert(type(math.sin) == types.func)
