function assert_not(truth, desc = '') {
	assert(!truth, desc)
}

function assert_error(fn, desc = '') {

	was_error = true
	try {
		fn()
		was_error = false
	} catch {
		// Pass.
	}

	assert(was_error, desc)
}
